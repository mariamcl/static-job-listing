import React, { Component } from 'react';

class FilterBar extends Component {
    render() {
        return <div>
            Here's where filters go
        </div>
    }
}

export default FilterBar;